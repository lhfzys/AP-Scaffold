<UserControl
    x:Class="AP.Plugin.DeviceConfiguration.Views.ScannerSettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:prism="http://prismlibrary.com/"
    xmlns:system="clr-namespace:System;assembly=mscorlib"
    xmlns:ui="clr-namespace:AP.Shared.UI.Behaviors;assembly=AP.Shared.UI"
>

    <Grid ui:PermissionBehavior.Permission="device.config"
          ui:PermissionBehavior.HideWhenUnauthorized="False">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.Resources>
            <Style TargetType="TextBlock" BasedOn="{StaticResource MaterialDesignBody1TextBlock}">
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="Margin" Value="0,0,16,0"/>
            </Style>
            <Style TargetType="TextBox" BasedOn="{StaticResource MaterialDesignTextBox}">
                <Setter Property="Margin" Value="0,8"/>
            </Style>
            <Style TargetType="ComboBox" BasedOn="{StaticResource MaterialDesignComboBox}">
                <Setter Property="Margin" Value="0,8"/>
            </Style>
        </Grid.Resources>

        <Grid Grid.Row="0" Margin="0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="启用扫码枪"/>
            <CheckBox Grid.Column="1"
                      IsChecked="{Binding Enabled}"
                      Content="取消勾选并重启后，不再连接扫码枪（状态栏也不再显示该设备）"
                      VerticalAlignment="Center"/>
        </Grid>

        <Grid Grid.Row="1" Margin="0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="通信串口"/>
            <TextBox Grid.Column="1"
                     md:HintAssist.Hint="例如：COM1"
                     Text="{Binding PortName, UpdateSourceTrigger=PropertyChanged}" />
        </Grid>

        <Grid Grid.Row="2" Margin="0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="波特率"/>
            <ComboBox Grid.Column="1" SelectedItem="{Binding BaudRate}">
                <system:Int32>9600</system:Int32>
                <system:Int32>19200</system:Int32>
                <system:Int32>38400</system:Int32>
                <system:Int32>57600</system:Int32>
                <system:Int32>115200</system:Int32>
            </ComboBox>
        </Grid>

        <Grid Grid.Row="3" Margin="0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="数据位"/>
            <TextBox Grid.Column="1" Text="{Binding DataBits, UpdateSourceTrigger=PropertyChanged}"/>
        </Grid>

        <Grid Grid.Row="4" Margin="0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="校验位"/>
            <ComboBox Grid.Column="1" SelectedItem="{Binding Parity}">
                <system:String>None</system:String>
                <system:String>Odd</system:String>
                <system:String>Even</system:String>
                <system:String>Mark</system:String>
                <system:String>Space</system:String>
            </ComboBox>
        </Grid>

        <Grid Grid.Row="5" Margin="0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="停止位"/>
            <ComboBox Grid.Column="1" SelectedItem="{Binding StopBits}">
                <system:String>One</system:String>
                <system:String>OnePointFive</system:String>
                <system:String>Two</system:String>
            </ComboBox>
        </Grid>

        <Grid Grid.Row="6" Margin="0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="换行符"/>
            <ComboBox Grid.Column="1"
                      SelectedValue="{Binding NewLine}"
                      SelectedValuePath="Tag">
                <ComboBoxItem Tag="\r" Content="CR (\r)"/>
                <ComboBoxItem Tag="\n" Content="LF (\n)"/>
                <ComboBoxItem Tag="\r\n" Content="CRLF (\r\n)"/>
            </ComboBox>
        </Grid>

        <TextBlock Grid.Row="7"
                   Text="* 修改以上配置后需要重新连接扫码枪才能生效"
                   Foreground="{DynamicResource MaterialDesignBodyLight}"
                   FontSize="12"
                   Margin="0,24,0,0"
                   VerticalAlignment="Bottom"/>
    </Grid>
</UserControl>
