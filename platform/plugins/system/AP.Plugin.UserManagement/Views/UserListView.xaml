<UserControl x:Class="AP.Plugin.UserManagement.Views.UserListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900"
             Background="{DynamicResource MaterialDesignBackground}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 标题栏 -->
        <md:ColorZone Grid.Row="0" Mode="PrimaryMid" Padding="20,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="用户管理"
                           FontSize="20"
                           FontWeight="Bold"
                           Foreground="White"
                           VerticalAlignment="Center"/>
                <Button Grid.Column="1"
                        Style="{StaticResource MaterialDesignToolForegroundButton}"
                        Command="{Binding LoadUsersCommand}"
                        ToolTip="刷新">
                    <md:PackIcon Kind="Refresh" Width="22" Height="22" Foreground="White"/>
                </Button>
            </Grid>
        </md:ColorZone>

        <!-- 工具栏 -->
        <Border Grid.Row="1" Background="{DynamicResource MaterialDesignCardBackground}" Padding="16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0"
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         md:HintAssist.Hint="搜索用户名或显示名"
                         md:TextFieldAssist.HasLeadingIcon="True"
                         md:TextFieldAssist.LeadingIcon="Magnify"
                         Width="300"
                         HorizontalAlignment="Left"/>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="新增用户"
                            Style="{StaticResource MaterialDesignRaisedButton}"
                            Command="{Binding AddUserCommand}"
                            Margin="0,0,12,0"/>
                    <Button Content="编辑"
                            Style="{StaticResource MaterialDesignOutlinedButton}"
                            Command="{Binding EditUserCommand}"
                            Margin="0,0,12,0"/>
                    <Button Content="重置密码"
                            Style="{StaticResource MaterialDesignOutlinedButton}"
                            Command="{Binding ResetPasswordCommand}"
                            Margin="0,0,12,0"/>
                    <Button Content="删除"
                            Style="{StaticResource MaterialDesignOutlinedButton}"
                            Command="{Binding DeleteUserCommand}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 数据表格 -->
        <Border Grid.Row="2" Padding="16">
            <DataGrid ItemsSource="{Binding Users}"
                      SelectedItem="{Binding SelectedUser}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      GridLinesVisibility="Horizontal"
                      BorderThickness="0"
                      Background="{DynamicResource MaterialDesignPaper}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="用户名" Binding="{Binding UserName}" Width="150"/>
                    <DataGridTextColumn Header="显示名" Binding="{Binding DisplayName}" Width="200"/>
                    <DataGridTemplateColumn Header="角色" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <ItemsControl ItemsSource="{Binding Roles}" Margin="0,4">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <WrapPanel />
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Background="{DynamicResource PrimaryHueLightBrush}"
                                                    CornerRadius="4"
                                                    Padding="6,2"
                                                    Margin="0,0,6,0">
                                                <TextBlock Text="{Binding}" FontSize="12" Foreground="White"/>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridCheckBoxColumn Header="启用" Binding="{Binding IsEnabled}" Width="80"/>
                    <DataGridCheckBoxColumn Header="需改密" Binding="{Binding MustChangePassword}" Width="90"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- 加载遮罩 -->
        <Grid Grid.Row="0" Grid.RowSpan="3"
              Background="#AAFFFFFF"
              Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}"
                             Value="0"
                             IsIndeterminate="True"
                             Width="50" Height="50"/>
                <TextBlock Text="{Binding BusyText}"
                           Margin="0,16,0,0"
                           HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
